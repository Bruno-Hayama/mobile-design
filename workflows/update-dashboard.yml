name: Atualizar Dashboard Mobile Design

on:
  schedule:
    # Toda segunda-feira às 08:00 horário de Brasília (11:00 UTC)
    - cron: '0 11 * * 1'
  # Permite rodar manualmente pelo GitHub
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Rodar script de atualização
        run: python update_dashboard.py

      - name: Commit e push do dashboard atualizado
        run: |
          git config --global user.name "Dashboard Bot"
          git config --global user.email "bot@mobile-design.com"
          git add index.html
          git diff --staged --quiet || git commit -m "🤖 Dashboard atualizado automaticamente $(date +'%d/%m/%Y %H:%M')"
          git push
